# ============================================
# API Configuration for Admin Management Panel
# ============================================

# Base URL for the backend API (without version)
# Development: http://127.0.0.1:8001 or http://localhost:8001
# Production: https://www.testcontent.innovatedemo.com/api
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8001

# API version to use (appended to base URL)
# Current version: v1.0
# Full API URL will be: {NEXT_PUBLIC_API_BASE_URL}/{NEXT_PUBLIC_API_VERSION}
NEXT_PUBLIC_API_VERSION=v1.0

# External API for Dashboard Stats (optional)
# Used for fetching dashboard statistics from external source
NEXT_PUBLIC_EXTERNAL_STATS_URL=https://www.testcontent.innovatedemo.com/v1.0

# ============================================
# Authentication Configuration
# ============================================

# LocalStorage key for storing authentication token
# The token is used in Authorization header as: Bearer {token}
# Default: admin_auth_token
NEXT_PUBLIC_TOKEN_STORAGE_KEY=admin_auth_token

# LocalStorage key for storing refresh token (if applicable)
# Default: admin_refresh_token
NEXT_PUBLIC_REFRESH_TOKEN_KEY=admin_refresh_token

# Enable user registration feature (true/false)
# Default: false
NEXT_PUBLIC_ENABLE_REGISTRATION=false

# ============================================
# Export Feature Configuration
# ============================================

# Export API endpoints are automatically configured based on:
# - NEXT_PUBLIC_API_BASE_URL
# - NEXT_PUBLIC_API_VERSION
#
# Export endpoints used:
# - POST {API_URL}/export/hotels - Create hotel export job
# - POST {API_URL}/export/mappings - Create mapping export job
# - GET {API_URL}/export/status/{job_id} - Get export job status
# - GET {API_URL}/export/download/{job_id} - Download completed export
#
# All export requests require authentication token in Authorization header

# ============================================
# Application Configuration
# ============================================

# Application display name
NEXT_PUBLIC_APP_NAME=Admin Management Panel

# Application version
NEXT_PUBLIC_APP_VERSION=1.0.0

# ============================================
# Development Configuration
# ============================================

# Node environment (development, production, test)
NODE_ENV=development

# Enable mock authentication for development (true/false)
# When true, bypasses real API authentication
# Default: false
NEXT_PUBLIC_USE_MOCK_AUTH=false

# Enable mock API responses for development (true/false)
# When true, returns mock data instead of calling real API
# Default: false
NEXT_PUBLIC_USE_MOCKS=false

# ============================================
# Production Configuration Notes
# ============================================
#
# For production deployment, ensure:
# 1. NEXT_PUBLIC_API_BASE_URL points to production API server
# 2. NEXT_PUBLIC_USE_MOCK_AUTH is set to false
# 3. NEXT_PUBLIC_USE_MOCKS is set to false
# 4. NODE_ENV is set to production
# 5. Backend CORS is configured to allow requests from your domain
# 6. Backend API endpoints are accessible and properly secured
#
# Example production configuration:
# NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com
# NEXT_PUBLIC_API_VERSION=v1.0
# NEXT_PUBLIC_USE_MOCK_AUTH=false
# NEXT_PUBLIC_USE_MOCKS=false
# NODE_ENV=production