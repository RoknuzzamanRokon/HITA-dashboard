"use client";

import React from "react";
import CachedProfilePage from "./page-cached";

// Export the cached version as the default profile page
export default function ProfilePage() {
  return <CachedProfilePage />;
}
